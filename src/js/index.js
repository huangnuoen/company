/*$('p.ellipsis').each(function(){
	var maxWidth = 40;
	if($(this).text().length > maxWidth) {
		$(this).text($(this).text().substring(0, maxWidth));
		$(this).html($(this).html() + '…');
	}
})
*/function maxWidth() {
	
}