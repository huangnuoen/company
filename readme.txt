*文件、目录名全为小写
源代码放在src文件夹中
src
--scss放sass文件
--css放编译后的css文件
--js 所有js文件
--img放网站所需要的所有图片
--font所有需要的字体
--index.html
第三方库放在lib中
lib
--bootstrap

压缩后的代码放在dist中
dist（distribution）
--css 压缩后的css
--js 合并压缩后的js
--img 压缩优化后的img